import org.junit.Test;
import static org.junit.Assert.*;

public class DriverTest
{

Term polynomial1 = {    new Term(3,2),
                        new Term(2,1),
                        new Term(1,0)};

String terms = {"3x^2", "2x", "1"};




@Test
public void addPolynomialToEmptyTest()
{
    Polynomial Test = new Polynomial;
    Polynomial empty = newPolynomial;

    for(int i = 0; i < polynomial1.length; i++)
    {
        test.addTerm(polynomial1[i]);
    }

    test.add(empty);

    assertEquals(test, polynomial1);
}


@Test
public void addTermTest()
{
    for(int i =0; i < terms.length; i++)
    {
        Term testTerm = new Term(polynomial[i].getcoefficient(), polynomial[i].getExponent());

        assertEquals("The test toSrting and the expected toString do not match", testTerm.toString(), polynomial[i].toString());
    }
}


}